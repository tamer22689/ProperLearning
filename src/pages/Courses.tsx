import { Flex } from "@chakra-ui/react";
import CoursesCards from "../cards/CoursesCards";

const Courses = () => {
  return (
    <Flex>
        <CoursesCards/>
    </Flex>
  );
};

export default Courses;
